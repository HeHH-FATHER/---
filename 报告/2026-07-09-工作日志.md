# 逐风数据洞察平台 — 工作日志（2026-07-09）

> 主题：集群联调 + Java 生成器部署 + 链路监控
> 集群：6 节点全在线

---

## 一、上午完成任务

| # | 任务 | 状态 |
|---|------|:--:|
| 1 | 集群恢复 + 四条链路全启动 | ✅ |
| 2 | 修复生成器版本编码（LANG=UTF-8）| ✅ |
| 3 | 生成器加 --loop 持续模式 | ✅ |
| 4 | 编译上传 gacha MR jar 到 master0 | ✅ |
| 5 | 编译上传 satisfaction Java jar | ✅ |
| 6 | 满意度消费端编写+部署（Kafka→Redis）| ✅ |
| 7 | 大屏抽卡面板改读离线 MySQL | ✅ |
| 8 | 大屏 404 清理（去掉 build/funnel 未实现接口）| ✅ |
| 9 | 链路监控面板（替换流失对比）| ✅ |
| 10 | 满意度分数修复（三维全≤10 + 均值回归→对齐Python）| ✅ |
| 11 | 启动指南/部署方案更新 | ✅ |
| 12 | RuoYi 打包 | ✅ |

## 二、集群启动问题与解决

| 问题 | 解决 |
|------|------|
| 生成器 `--version 6.6下半` 乱码 | `export LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8` |
| `nohup java` 找不到 java | 使用全路径 `/root/jdk1.8.0_171/bin/java` |
| `nohup` 把 `-D` 当自己参数 | 用 `&` + `disown` 替代 nohup |
| 生成器只跑一次就停 | 命令必须加 `--loop` 参数 |
| 满意度分数全漂到10分 | 均值回归改为对齐 Python 的平衡随机游走，清空旧 ZSET 数据 |
| 满意度消费者缺 star/ability/look | 消费者加缓存字典补全字段 |
| `hsan mapping` 不支持 Redis 3.0 | 改用 `hmset` |
| Java jar 不含 satisfaction 类 | 重新 `mvn package` 编译 |
| GachaAggMR.java 注释 `*/5` 误解析 | 改 cron 示例为中文注释 |

## 三、四条链路最终状态

| 链路 | 生成器 | 位置 | 状态 |
|---|------|------|:--:|
| 深渊离线 | Abyss-Record-Generator (Java) | master0 | ⏸ |
| 练度实时 | CharacterBuildGenerator (Java) → Kafka → Python Consumer → Redis | master0→Middleware | ✅ |
| 抽卡离线 | GachaPullSimulator (Java) → HDFS → MR → MySQL | master0 | ✅ |
| 满意度实时 | SatisfactionProducerApp (Java) → Kafka → Python Consumer → Redis | master0→Middleware | ✅ |

## 四、新增文件

| 文件 | 用途 |
|------|------|
| `scripts/start_generators.sh` | 一键启动所有 Java 生成器 |
| `scripts/satisfaction_consumer.py` | 满意度 Kafka 消费端（写 Redis）|
| `scripts/monitor_collector.py` | 链路监控数据采集（hmset → Redis）|
| `mr_jobs/.../CleanGachaMR.java` | 抽卡 DWD 过滤 MR |
| `mr_jobs/.../GachaAggMR.java` | 抽卡 DWS 聚合 MR |
| `Character-Build-Generator/.../satisfaction/` | 满意度 Java 生成器（3文件）|

## 五、文档更新

- `启动指南.md`：更新生成器启动命令（Java + loop + 全路径 + UTF-8）
- `部署操作手册.md`：更新四条链路架构 + 故障排查
- `报告/明日全链路启动清单.md`：更新上线状态

---

## 六、下午完成任务（Spark Streaming + 全节点监控 + 离线管道整合）

| # | 任务 | 状态 |
|---|------|:--:|
| 13 | **Spark Streaming 替代 Python 消费端** | ✅ |
| 14 | Spark 集群模式部署（spark.cores.max=1 双作业各占 1 Core） | ✅ |
| 15 | 解决序列化问题（3 个 Model 类 + Commons-pool2 依赖） | ✅ |
| 16 | 解决 Satisfaction 34,848 条历史回放（consumer group + offset reset） | ✅ |
| 17 | **全节点监控增强**（4→14 指标，5→6 节点，含 vserver） | ✅ |
| 18 | CPU 从 loadavg 改为真实百分比（load÷nproc×100） | ✅ |
| 19 | Redis 内存从 0% 改为绝对 MB + key 数量 | ✅ |
| 20 | 前端监控面板重设计（三行布局 + 2×3 节点网格） | ✅ |
| 21 | 抽卡离线管道循环模式修复（Python pipe → shell while loop） | ✅ |
| 22 | 深渊离线管道搭建（Java 生成器 + StatsDocument 构建 + ADS 入库） | ✅ |
| 23 | 一键启动覆盖全部链路（实时+离线） | ✅ |
| 24 | 文档同步（启动指南 + 部署手册 + Kafka offset 自动重置） | ✅ |

## 七、下午关键技术问题与解决

| 问题 | 根因 | 解决 |
|------|------|------|
| Spark 作业不显示在 UI | `local[2]` 模式不经过 Master | 改 `spark://master0:7077` + `--conf spark.cores.max=1` |
| 大屏练度不滚动 | foreachPartition 批量 LPUSH 后一次性 LTRIM | 逐条 LPUSH+LTRIM（匹配 Python 行为） |
| Satisfaction 不更新 | 旧 consumer group 从 earliest 回放 34,848 条 | `spark-satisfaction-v2` + `kafka-consumer-groups --reset-offsets` |
| `NotSerializableException` | Model 类未实现 Serializable | CharacterBuildRecord/WeaponChoice/ArtifactSetChoice/SatisfactionRecord |
| `NoClassDefFoundError: commons-pool2` | Jedis 连接池依赖缺失 | 下载 commons-pool2-2.4.2.jar |
| Satisfaction 抢 2 Core，Build 无资源 | Spark 默认分配所有可用 Core | `--conf spark.cores.max=1` |
| 抽卡管道循环卡死 | `--loop` pipe 链下游退出后管道断裂 | shell `while true; do ...; sleep 300; done` |
| CPU 显示 loadavg（如 1.0）无法理解 | `/proc/loadavg` 是负载非百分比 | `load ÷ nproc × 100` 转真实使用率 |
| Redis 内存永远 0% | 677KB ÷ 256MB = 0.26% | 显示绝对内存（MB）+ key 数 |
| 监控"消费端 0 进程" | Python 消费端已停用 | `streaming_count`（Spark 作业数） |
| 头像跨机器访问消失 | CDN 防盗链（localhost 正常，IP 访问被拦） | 待解决：后端代理或换图源 |

## 八、最终集群状态

### 实时链路（Spark Streaming 集群模式）
```
slave1 (1 core) → BuildStreamingConsumer         → Redis build:recent + build:hot_chars
slave2 (1 core) → SatisfactionStreamingConsumer  → Redis satisfaction:*
Spark UI: 2 Running Applications ✅
```
### 离线管道（master0，循环模式）
```
抽卡: run_gacha_loop.sh   (每5分钟 5000抽)
   preprocess_gacha → CleanGachaMR → ComputeGachaStats → load_gacha_to_mysql
   → MySQL ads_gacha_offline

深渊: run_abyss_loop.sh   (每10分钟 200玩家)
   build_abyss_stats → Abyss-Record-Generator.jar → preprocess_abyss → load_abyss_to_mysql
   → MySQL ads_char_summary + ads_team_usage
```
### 监控（6 节点 14 指标）
```
master0:    monitor_master0.py → SSH→slave1/2/backup/vserver → Redis screen:monitor
Middleware: monitor_collector.py → Kafka/Redis/系统 → Redis screen:monitor
前端: 三行布局（生成器+Spark / 消息+存储 / 6节点2×3网格）
```
### 一键启动
```bash
cd /root/abyss-pipeline && bash start_generators.sh            # 生成器 + 离线管道
cd /root/abyss-pipeline && bash scripts/start_spark_streaming.sh # Spark 消费端
```

## 九、新增/修改文件（下午）

| 文件 | 用途 |
|------|------|
| `Character-Build-Generator/.../streaming/CharacterBuildStreamingConsumer.java` | Spark 练度消费端（逐条 LPUSH+LTRIM） |
| `Character-Build-Generator/.../streaming/SatisfactionStreamingConsumer.java` | Spark 满意度消费端 |
| `Character-Build-Generator/.../model/*.java` | 4 个 Model 类 +Serializable |
| `Character-Build-Generator/.../StreamingConsumerTest.java` | 12 个单元测试 |
| `scripts/monitor_master0.py` | 6 节点监控采集（HDFS+Spark+系统） |
| `scripts/monitor_collector.py` | MW 侧监控（Kafka+Redis+系统） |
| `scripts/start_spark_streaming.sh` | Spark Streaming 一键启动（含 offset reset） |
| `scripts/run_gacha_loop.sh` | 抽卡循环管道（每5分钟） |
| `scripts/run_abyss_loop.sh` | 深渊循环管道（每10分钟） |
| `scripts/build_abyss_stats.py` | API 数据 → StatsDocument JSON |
| `scripts/load_abyss_to_mysql.py` | 深渊聚合+MySQL ADS 写入 |
| `scripts/start_offline_pipelines.sh` | 离线管道一键启动 |
| `scripts/start_generators.sh` | 更新：末尾调用离线管道 |
| `启动指南.md` | Spark 集群模式 + 离线管道 + jar 依赖 |
| `部署操作手册.md` | 实时链路重写 + 离线管道 + 故障排查 |

## 十、Git 提交（下午）

| Commit | 内容 |
|--------|------|
| `e0c8b1b` | Spark Streaming 替代 Python + 全节点监控 + 文档同步 |
| `551d192` | Spark Streaming Java 消费端 + 序列化修复 + 单元测试 |
| `3c224b4` | 启动指南/部署手册 + Kafka offset 自动重置 |
| `bc77c64` | 离线管道整合 + 一键启动 |

## 十一、晚上完成任务（离线管道 + 数据质量 + 全链路收尾）

| # | 任务 | 状态 |
|---|------|:--:|
| 27 | 抽卡管道修复：循环模式卡死 → shell while loop | ✅ |
| 28 | 抽卡五星率修复：`--players` 参数未传入 → 默认5000人→0.6% | ✅ |
| 29 | 抽卡五星率校准：50人/5000抽 → ~1.4% 接近真实 | ✅ |
| 30 | 抽卡 UP 权重：玛薇卡79.77%/洛恩20.23%，焚曜千阳62.05%/灾悔37.95% | ✅ |
| 31 | 抽卡面板累积显示：LIMIT 100→10000 + 前端跨批次合并 | ✅ |
| 32 | 深渊管道部署：build_abyss_stats + Java生成器 + loader→MySQL | ✅ |
| 33 | 深渊 100 支真实配队替换：v59 文件覆盖，按使用率加权 | ✅ |
| 34 | 深渊 team_name 修复：从 stats 索引查名 → 成员拼接统一 | ✅ |
| 35 | 深渊 use_rate/has_rate 修正：分母=拥有全员人数 + 使用=使用/拥有 | ✅ |
| 36 | **抽卡数据质量页重写**：ODS→DWD→DWS→ADS 管道可视化 + 清洗规则 | ✅ |
| 37 | **深渊数据质量页新建**：KPI + 管道流程 + 角色 TOP15 | ✅ |
| 38 | hotChars API 超时修复：40 次单查→3 次批量查（25s→1s） | ✅ |
| 39 | gacha quality API 字段修复：latest_five_rate→avg_five_rate | ✅ |
| 40 | satisfaction:top 修复：ability/look 硬编码→读取 Kafka 消息 | ✅ |
| 41 | Satisfaction Streaming 稳定性修复：foreachPartition + windowed TOP6 | ✅ |
| 42 | 环形图修复：无数据占位 + type 字段补全 + UP 过滤 | ✅ |
| 43 | **start_all.sh / stop_all.sh**：一键启停全链路（7步） | ✅ |
| 44 | Druid 慢 SQL 日志关闭 | ✅ |
| 45 | 文档最终同步 + 工作日志 | ✅ |

## 十二、晚上关键技术问题与解决

| 问题 | 根因 | 解决 |
|------|------|------|
| 抽卡五星率 0.6% | `--players` 参数未传入 generate_pulls() | 修复 main() 传参 + 默认 50 人 |
| 管道循环卡死 | `--loop` 内部 pipeline 下游一次性退出 | shell `while true; do ...; sleep 300; done` |
| 深渊配队名全是 "?" | team_index 查 stats 与 members 顺序不一致 | 统一用 members 拼接 |
| 使用率 200% | 分母用 total(200) 而非团队总选择次数 | team_count / sum(team_count) × 100 |
| 环形图消失 | 数据清空后 `!items.length` 直接 return | 无数据时显示占位环 |
| satisfaction 分数三个一样 | 代码硬编码 ability=satify, look=satify | 加 ROLE_CACHE 从 Kafka 消息读取 |
| Druid 慢日志刷屏 | slow-sql-millis=1000 记录批量查询 | log-slow-sql:false |

## 十三、最终全链路架构

```
master0:  start_all.sh 一键启动
  ├─ 生成器: Build(10条/s) + Satisfaction(3s/轮)
  ├─ 离线: 抽卡(5min/5000抽/50人/~1.4%) + 深渊(10min/200人/100实数队)
  ├─ Spark Streaming: BuildStreaming + SatisfactionStreaming (集群各1核)
  └─ 监控: monitor_master0.py (6节点CPU%)

Middleware:
  ├─ MySQL + Redis + Kafka + ZK
  └─ monitor_collector.py

slave1/2/backup: Spark Worker + DataNode
vserver: 待部署 RuoYi 后端
```

## 十四、今日 Git 提交汇总

| Commit | 内容 |
|--------|------|
| `e0c8b1b` | Spark Streaming 替代 Python + 全节点监控 + 文档同步 |
| `551d192` | Spark Streaming Java 消费端 + 序列化修复 + 单元测试 |
| `3c224b4` | 启动指南/部署手册 + Kafka offset 自动重置 |
| `bc77c64` | 离线管道整合 + 一键启动 |
| `8d11c84` | CPU 炸弹修复 + 全链路一键启动 + Kafka offset 修复 |
| `6e74ad1` | 部署手册残留修正 |
| `aa3a4da` | 一键启动 start_all.sh 写入文档 |
| `18ac1ec` | start_all.sh 第0步全局杀进程 |
| `21f6307` | start_all.sh 健康监测 + monitor 远程采集降频 |
| `501d4ab` | stop_all.sh 一键停止 |
| `b2095ce` | 下午工作日志 |
| `2002f0e` | gacha 50人 1.38%五星率 |
| `277597e` | Satisfaction 回退稳定 collect 方案 |
| `485bc2f` | 启动指南/部署手册最终版 |

---
> 📅 日期：2026-07-09
> ✍️ 记录人：Claude Code
