# 逐风数据洞察平台 — 部署操作手册

> 最后更新：2026-07-12 v3.0（vserver生产部署 + 练度/满意度/深渊三链路 + AI智能分析 + 监控全自动）

---

## 〇、前置条件

### 集群全部节点开机

```
master0    100.84.184.73    开机
slave1     100.87.187.124   开机
slave2     100.103.68.17    开机
Middleware 100.103.177.85   开机
backup     100.76.210.59    开机
vserver    100.74.215.12    开机
```

### 启动顺序

**一键启动**（master0 上执行，自动 SSH 到 Middleware）：
```bash
cd /root/abyss-pipeline && bash start_all.sh
```

**一键停止**：
```bash
cd /root/abyss-pipeline && bash stop_all.sh
```

7 步流程（start_all.sh 每步有状态校验）：
```
[1/7] 清理旧进程
[2/7] Middleware: MySQL → Redis → ZK → Kafka（SSH 远程，含 pip3 install redis）
[3/7] master0:    Hadoop HDFS + YARN + ZK + Spark
[4/7] master0:    Java 生成器（练度50条/s + 满意度3s/轮）
[5/7] master0:    离线管道（抽卡每5min循环 + 深渊每10min循环）
[6/7] master0:    Spark Streaming 消费端（清 checkpoint 防 offset 冲突）
[7/7] 双节点:     链路监控（先写 Redis 信标保证大屏立刻有数据）
```

---

## 一、程序清单 & 上传目标

### 1.1 离线链路（循环模式，start_all.sh 自动启动）

| 程序 | 文件位置 | 执行位置 | 频率 |
|------|---------|:--:|------|
| 抽卡管道 | `scripts/run_gacha_loop.sh` | master0 | 每 5 分钟 / 5000 抽 / 50 玩家 |
| 深渊管道 | `scripts/run_abyss_loop.sh` | master0 | 每 10 分钟 / 200 玩家 |
| Stats 构建 | `scripts/build_abyss_stats.py` | master0 | 100 支真实配队 |
| 深渊 ADS | `scripts/load_abyss_to_mysql.py` | master0 | 含头像 + 使用率/持有率 |

### 1.2 实时链路（Java 生成器 → Kafka → Spark Streaming → Redis）

| 程序 | 文件位置 | 执行位置 |
|------|---------|:--:|
| 练度生成器 | `Character-Build-Generator/` (Java) | master0 |
| 满意度生成器 | `SatisfactionProducerApp` (Java, 需创建 satify_data.json 软链接) | master0 |
| **Spark Streaming 消费端** | `Character-Build-Generator/` (Java) | master0 → Spark Workers |
| 链路监控采集（MW侧） | `scripts/monitor_collector.py` | Middleware |
| 链路监控采集（M0侧） | `scripts/monitor_master0.py` 🆕 | master0 |
| 一键启动生成器 | `scripts/start_generators.sh` | master0 |
| 一键启动消费端 | `scripts/start_spark_streaming.sh` 🆕 | master0 |

**架构**：
```
Character-Build-Generator ──→ Kafka(build-v2) ──→ Spark Streaming (BuildStreamingConsumer)
                                                     ├─→ Redis build:recent (LIST, LTRIM20)
                                                     └─→ Redis build:hot_chars (STRING, TOP4)

SatisfactionProducerApp(Java) ──→ Kafka(satisfaction-v1) ──→ Spark Streaming (SatisfactionStreamingConsumer)
                                                                ├─→ Redis rt:satisfaction:ranking (ZSET, 121)
                                                                ├─→ Redis rt:satisfaction:trend:* (LIST, LTRIM60)
                                                                └─→ Redis satisfaction:top (STRING, TOP6)

master0: monitor_master0.py → SSH→slave1/2/backup/vserver → hmset→Redis screen:monitor
Middleware: monitor_collector.py（守护进程自动重启）→ 本地Kafka/Redis/系统 → hmset→Redis screen:monitor
Redis 信标：start_all.sh 先写入全字段默认值，大屏立刻显示，监控数据随后覆盖
```

### 1.3 可视化

| 程序 | 文件位置 | 执行位置 |
|------|---------|:--:|
| RuoYi 后端（开发） | `ruoyi-admin/` | Windows IDEA |
| RuoYi 前端（开发） | `ruoyi-ui/` | Windows npm |
| RuoYi 后端（生产） | `/opt/ruoyi/ruoyi-admin.jar` | vserver (100.74.215.12) |
| RuoYi 前端（生产） | `/opt/ruoyi-ui/dist/` | vserver Nginx :80 |
| 数据大屏 | `ruoyi-ui/src/views/screen/` | 15+ 面板 |
| 角色/配队/练度/卡池分析 | `ruoyi-ui/src/views/analysis/` | 10+ 页面 |
| AI 智能分析 | AIController.java → 硅基流动API | vserver(走master0代理) / Windows(直连) |
| 数据管理 | `ruoyi-admin/.../controller/data/` | 8张 DIM/ADS 表 CRUD |
| 数据管理页 | `表文件/*/` → RuoYi 项目 | Windows |

### 1.4 上传命令

```bash
cd C:/Users/82165/Desktop/md

# === 传到 master0 ===
scp -r scripts/                          root@100.84.184.73:/root/abyss-pipeline/
scp -r mr_jobs/                          root@100.84.184.73:/root/abyss-pipeline/
scp -r Abyss-Record-Generator/           root@100.84.184.73:/root/abyss-pipeline/
scp -r Gacha-Record-Generator/           root@100.84.184.73:/root/abyss-pipeline/
scp -r Character-Build-Generator/        root@100.84.184.73:/root/abyss-pipeline/
scp -r 提瓦特数据/                        root@100.84.184.73:/root/abyss-pipeline/
# Spark Streaming 依赖 jar（首次需上传）
scp spark-streaming-kafka-0-10_2.11-2.4.0.jar root@100.84.184.73:/root/abyss-pipeline/jars/

# === 传到 Middleware ===
scp scripts/monitor_collector.py            root@100.103.177.85:/root/abyss-pipeline/scripts/
scp scripts/monitor_master0.py              root@100.84.184.73:/root/abyss-pipeline/scripts/

# === 依赖安装 ===
ssh root@100.84.184.73 "pip3 install redis"   # monitor_master0.py 用
ssh root@100.103.177.85 "pip3 install redis"   # monitor_collector.py 用
```

---

## 二、离线链路操作（在 master0 执行）

### 第1步：编译

```bash
cd /root/abyss-pipeline/mr_jobs
mvn clean package -DskipTests
```

### 第2步：生成深渊数据

```bash
cd /root/abyss-pipeline
java -jar Abyss-Record-Generator/Abyss-Record-Generator/target/abyss-record-generator-1.0-SNAPSHOT-jar-with-dependencies.jar \
  --stats 提瓦特数据/<stats.json> \
  --out output/ \
  --users 200 --dirty-rate 0.05
```

### 第3步：运行离线管道

```bash
bash scripts/run_offline_pipeline.sh \
  --input output/ \
  --version "v6.6"
```

或分步执行：
```bash
# ODS
python3 scripts/preprocess_abyss.py --input output/ --version v6.6

# DWD
hadoop jar abyss-mr.jar com.neu.abyss.AbyssCleanMR \
  /data/abyss/ods/abyss_v66.jsonl /data/abyss/tmp_clean

# DWS
hadoop jar abyss-mr.jar com.neu.abyss.AbyssAggMR \
  /data/abyss/dwd/dwd_char_detail/ /data/abyss/dws/dws_char_summary/
python3 scripts/compute_team_cooccur.py \
  /data/abyss/dwd/dwd_team_usage/ /data/abyss/dws/dws_team_cooccur/

# ADS
python3 scripts/load_ads_to_mysql.py \
  --host 100.103.177.85 --port 3306 \
  --user root --password 123456 --database abyss_db

# 验证
python3 scripts/verify_consistency.py --auto
```

---

## 二点五、离线抽卡管道（ODS→DWD→DWS→ADS · 每 5 分钟）

> 抽卡从实时链路迁至离线管道。生成器含保底机制（软74/硬90/50-50），MR 过滤五星后聚合入 MySQL。

### 架构

```
Gacha-Record-Generator (Java, 含保底)
    ↓ 生成逐抽记录 JSONL
preprocess_gacha.py → HDFS /data/gacha/ods/   (ODS)
    ↓
CleanGachaMR.java → HDFS /data/gacha/dwd/     (DWD 过滤五星)
    ↓
GachaAggMR.java → MySQL ads_gacha_offline     (DWS 聚合 + ADS 写入)
    ↓
数据质量页 (/analysis/quality) 监控批次
```

### 程序清单

| 层 | 程序 | 执行位置 |
|---|------|:--:|
| ODS | `scripts/preprocess_gacha.py` | master0 |
| DWD | `mr_jobs/.../CleanGachaMR.java` | master0 (YARN) |
| DWS | `mr_jobs/.../GachaAggMR.java` | master0 (YARN) |
| ADS | `scripts/load_gacha_to_mysql.py` | master0 |

### 管道命令

```bash
# 完整管道（Python 仿真版，无需 Hadoop）
cd /root/abyss-pipeline
python3 scripts/preprocess_gacha.py --count 5000 \
  | python3 scripts/CleanGachaMR.py \
  | python3 scripts/ComputeGachaStats.py 5000 \
  | python3 scripts/load_gacha_to_mysql.py

# 循环模式（每 5 分钟一批）
python3 scripts/preprocess_gacha.py --loop 300 --count 5000 \
  | python3 scripts/CleanGachaMR.py \
  | python3 scripts/ComputeGachaStats.py 5000 \
  | python3 scripts/load_gacha_to_mysql.py

# Hadoop 集群模式（集群可用时）
hadoop jar gacha-mr.jar com.neu.gacha.CleanGachaMR /data/gacha/ods/ /data/gacha/dwd/
hadoop jar gacha-mr.jar com.neu.gacha.GachaAggMR /data/gacha/dwd/
```

### 验证

```bash
# 检查 MySQL 数据
mysql -h100.103.177.85 -uroot -p123456 abyss_db \
  -e "SELECT batch_time, five_rate, total_pulls FROM ads_gacha_offline WHERE item_name='__ALL__' ORDER BY batch_time DESC LIMIT 5"

# 数据质量 API
curl http://localhost:8080/analysis/gacha/quality
```

---

## 三、实时链路操作（2026-07-09 · Spark Streaming 版）

> ⚠️ **Python 消费端已全部替换为 Spark Streaming。旧 Python 代码备份在 `实时链路备份/共享消费端/`。**

### 架构

```
                    start_generators.sh                  start_spark_streaming.sh
                    /                 \                  /                        \
Character-Build-Generator (Java)   SatisfactionProducerApp    BuildStreamingConsumer   SatisfactionStreamingConsumer
    --count 10 --loop                 --loop --interval 3      60s窗口, TOP4             per-record ZADD+LIST
         ↓                                ↓                        ↓                        ↓
    Kafka build-v2                  Kafka satisfaction-v1      Spark Workers (slave1/2)   Spark Workers
         ↓                                ↓                        ↓                        ↓
    BuildStreamingConsumer          SatisfactionStreamingConsumer  Redis build:*           Redis satisfaction:*
         ↓                                ↓
    Redis build:recent (LTRIM20)    Redis rt:satisfaction:ranking (ZSET, 121角色)
    Redis build:hot_chars (TOP4)    Redis satisfaction:top (TOP6 JSON)
```

### 链路参数速查

| 参数 | 练度 (Build) | 满意度 (Satisfaction) |
|------|-------------|---------------------|
| 生成器 | `Character-Build-Generator` (Java `--loop`) | `SatisfactionProducerApp` (Java `--loop`) |
| Kafka Topic | `build-v2` | `satisfaction-v1` |
| 生成频率 | 每秒 10 条 | 每 3 秒 1 轮 |
| 消费端 | **Spark Streaming** (`CharacterBuildStreamingConsumer`) | **Spark Streaming** (`SatisfactionStreamingConsumer`) |
| Consumer Group | `spark-build-v2` | `spark-satisfaction-v2` |
| 窗口 | 60s 滑动 / 3s flush | per-batch (3s) |
| Redis Keys | `build:recent` (LTRIM20) `build:hot_chars` | `rt:satisfaction:ranking` `satisfaction:top` |
| 数据源 | `build_stats.json` | `satify_data.json` → `提瓦特数据/角色满意度排行.json` |
| Spark Master | `spark://master0:7077`（`--conf spark.cores.max=1`） | `spark://master0:7077`（`--conf spark.cores.max=1`） |
| 所需 jars | `jedis-2.9.0.jar`, `kafka-clients-2.1.0.jar`, `spark-streaming-kafka-0-10_2.11-2.4.0.jar`, `commons-pool2-2.4.2.jar` | 同左 |
| Batch 间隔 | **1 秒**（逐条 LPUSH+LTRIM，保证滚动） | 3 秒 |
| ⚠️ 重启注意 | 清 checkpoint + 换新 consumer group，否则回放历史消息卡死 | 同上 |

---

### 第1步：确认 Spark + Kafka 就绪

```bash
# Spark Master（master0）
ssh root@master0 'ps aux | grep SparkSubmit | grep -v grep'
# Spark UI: http://master0:8080

# Kafka（Middleware）
ssh root@Middleware '/root/kafka2/bin/kafka-run-class.sh kafka.tools.GetOffsetShell --broker-list localhost:9092 --topic build-v2 --time -1'
```

### 第2步：启动生成器（master0 · 一键）

```bash
cd /root/abyss-pipeline && bash start_generators.sh
# → 练度生成器 + 满意度生成器
```

### 第3步：启动 Spark Streaming 消费端（master0 · 一键）

```bash
cd /root/abyss-pipeline && bash scripts/start_spark_streaming.sh
# → BuildStreamingConsumer + SatisfactionStreamingConsumer
# 自动清理 Middleware 上旧的 Python 消费端
```

### 第4步：验证

```bash
# Spark 作业（2 个 SparkSubmit 进程）
ps aux | grep SparkSubmit | grep -v grep

# Redis 实时数据
ssh root@Middleware 'redis-cli LLEN build:recent'         # 20
ssh root@Middleware 'redis-cli GET build:hot_chars'        # TOP4 JSON
ssh root@Middleware 'redis-cli GET satisfaction:top'       # TOP6 JSON
ssh root@Middleware 'redis-cli ZCARD rt:satisfaction:ranking'  # 121

# Spark UI
curl -s http://master0:4040/api/v1/applications  # Build 作业
curl -s http://master0:4041/api/v1/applications  # Satisfaction 作业
```

---

### 程序备份位置

| 文件 | 原始位置 | 备份位置 |
|------|---------|---------|
| 抽卡生成器源码 | `Gacha-Record-Generator/` | `实时链路备份/抽卡链路-Gacha/` |
| 练度生成器源码 | `Character-Build-Generator/` | `实时链路备份/练度链路-Build/` |
| 消费端 | `scripts/realtime_consumer.py` | `实时链路备份/共享消费端/` |
| 调度脚本 | `scripts/run_gen_loop.sh` | `实时链路备份/共享消费端/` |
| 快照落库 | `scripts/snapshot_rt_to_mysql.py` | `实时链路备份/共享消费端/` |
| 链路测试 | `scripts/test_realtime_chain.py` | `实时链路备份/共享消费端/` |

> 📌 **备份时间**：2026-07-07 · 锁定原因：两条链路端到端跑通，消费端格式兼容验证通过。

---

## 四、可视化链路操作

### 第1步：RuoYi 后端（Windows IDEA）

1. 打开 `D:\neu2026bd\projects\ruoyi\`
2. IDEA 导入 Maven 项目
3. 修改 `application-druid.yml`：数据库指向 `100.103.177.85:3306`
4. 运行 `RuoYiApplication.java` → `localhost:8080`

### 第2步：RuoYi 前端（Windows 终端）

```bash
cd D:\neu2026bd\projects\ruoyi\ruoyi-ui
npm install && npm run dev
# → localhost:8088
```

### 第3步：访问

```
数据大屏: http://localhost:80 → 数据大屏
API验证:  curl http://localhost:8080/api/offline/meta?type=red
```

### 大屏面板布局（2026-07-07）

```
┌──────────────┬────────────────────┬──────────────┐
│ 左栏 (7fr)   │ 中栏 (10fr)         │ 右栏 (7fr)   │
├──────────────┼────────────────────┼──────────────┤
│ 配队共现网络  │ 持有-使用率 TOP15   │ 热门配队 TOP8 │
│ (力导向图)    │ (头像+双柱图)       │ (人物卡片)    │
├──────────────┼────────────────────┼──────────────┤
│ 热门角色练度  │ 卡池抽取占比        │ 版本涨跌异动榜 │
│ (头像+武器+  │ (双环形图)          │ (哑铃图)      │
│  圣遗物卡片)  │                    │              │
├──────────────┼────────────────────┼──────────────┤
│ 全服实时练度  │                    │ 持有-使用     │
│ (滚动日志)    │                    │ 流失对比      │
└──────────────┴────────────────────┴──────────────┘
```

### 大屏新增数据表

| 表名 | 用途 | 数据来源 |
|------|------|---------|
| `dim_artifact` | 圣遗物图标维表 (114套) | 角色练度统计.json |
| `ads_team_usage` | 热门配队 TOP10 | 深渊配队汇总.json (6.6第一期) |
| `ads_char_momentum` | 角色版本涨跌异动 | 深渊配队汇总.json (跨版本对比) |

### 大屏新增 API

| 接口 | 数据源 | 面板 |
|------|--------|------|
| `GET /api/offline/teamTop` | ads_team_usage | 热门配队 TOP8 |
| `GET /api/offline/momentum` | ads_char_momentum | 版本涨跌异动榜 |
| `GET /api/realtime/hotChars` | Redis build:hot_chars → dim_weapon/dim_artifact 补图标 | 热门角色平均练度 |

---

## 五、验证清单

| # | 验证项 | 命令 | 期望结果 |
|---|--------|------|---------|
| 1 | HDFS 数据存在 | `hdfs dfs -ls /data/abyss/ods/` | 看到 `abyss_v66.jsonl` |
| 2 | MR 作业完成 | YARN UI `http://100.84.184.73:8088` | SUCCEEDED |
| 3 | MySQL ADS 有数据 | `mysql -h100.103.177.85 -uroot -p123456 abyss_db -e "SELECT COUNT(*) FROM ads_meta_ranking"` | 20 |
| 4 | ads_char_summary 全量 | `SELECT COUNT(*) FROM ads_char_summary` | 115 |
| 5 | ads_team_matrix 全量 | `SELECT COUNT(*) FROM ads_team_matrix` | 217 |
| 6 | Kafka 有消息 | ssh Middleware `/root/kafka2/bin/kafka-run-class.sh kafka.tools.GetOffsetShell --broker-list localhost:9092 --topic build-v2 --time -1` | 有数字 |
| 7 | Spark Streaming 运行中 | `ps aux \| grep SparkSubmit \| grep -v grep`（master0） | 2 个进程 |
| 8 | Redis build:recent | `redis-cli -h Middleware LLEN build:recent` | 20 |
| 8.5 | Redis build:hot_chars | `redis-cli -h Middleware GET build:hot_chars` | JSON 数组 TOP4 |
| 9 | RuoYi 后端正常 | `curl http://localhost:8080/api/offline/meta?type=red` | JSON |
| 10 | 大屏正常渲染 | `http://localhost:80` → 数据大屏 | 所有面板有数据 |
| 11 | 满意度数据 | `redis-cli -h Middleware ZCARD rt:satisfaction:ranking` | 121 |
| 12 | 满意度大屏 | 大屏左栏 → 实时角色满意度排行 | TOP6 + 涨跌 + 趋势图 |
| 13 | Spark 监控面板 | 大屏右栏 → 链路监控 → Spark 卡片 | 2 Worker \| 2 任务 |
| 14 | 集群节点监控 | 大屏右栏 → 链路监控 → 系统资源 | 6 节点 CPU/MEM |
| 15 | AI 智能分析 | 浏览器 → 数据分析 → AI 智能分析 | 可对话，返回数据引用 |
| 16 | vserver 前端 | `curl -sI http://100.74.215.12/` | HTTP 200 |
| 17 | 深渊批次 | `redis-cli GET abyss:batch_count` | 数字持续增长 |
| 18 | 监控信标 | `redis-cli HLEN screen:monitor` | >20 字段 |

---

## 六、常见问题

| 问题 | 原因 | 解决 |
|------|------|------|
| Spark Streaming 起不来 | spark-streaming-kafka jar 缺失 | 下载：`curl -sL -o /root/abyss-pipeline/jars/spark-streaming-kafka-0-10_2.11-2.4.0.jar 'https://repo1.maven.org/maven2/org/apache/spark/spark-streaming-kafka-0-10_2.11/2.4.0/spark-streaming-kafka-0-10_2.11-2.4.0.jar'` |
| `NoClassDefFoundError: LocationStrategies` | jar 不在 `--jars` 里 | 确认 spark-submit 带上 `--jars ...spark-streaming-kafka...jar` |
| `SatisfactionRecord not serializable` | 模型类未实现 Serializable | 重新编译 jar（已修复） |
| Python 消费端残留 | 旧脚本仍在运行 | `pkill -f realtime_consumer; pkill -f hot_char; pkill -f satisfaction_consumer` |
| Kafka 连不上 Middleware:9092 | Kafka 未启动 | `ssh Middleware '/root/kafka2/bin/kafka-server-start.sh -daemon /root/kafka2/config/server.properties'` |
| Kafka 启动失败 ZK timeout | ZK 未就绪 | 先启 ZK，等 5 秒再启 Kafka |
| vserver 外网不通 | 防火墙封 443 | master0 Python 代理 :3128 → SiliconFlow |
| vserver AI 无响应 | 代理挂了 | 重跑 start_all.sh 自动拉起代理 |
| vserver 磁盘满 | 日志堆积 | `rm -f /tmp/ruoyi.log` 清理 |
| vserver Nginx 报错 | gzip 重复配置 | `rm -f /etc/nginx/conf.d/gzip.conf && systemctl start nginx` |
| 练度 KPI 永远是 200 | jar 版本旧 | 重新 mvn package + scp jar + 清 checkpoint |
| 深渊批次不增长 | 循环脚本挂了 | `pkill -f run_abyss_loop` 后重跑 start_all.sh |
| 清洗率显示固定值 | 用了累计计算 | 批次明细表看独立值；KPI 看累计值 |
| 生成器 0 条记录 | version 中文过滤编码问题 | 去掉 `--version` 参数，或确认文件编码 UTF-8 |
| MySQL 拒绝连接 | 未授权远程访问 | `GRANT ALL ON *.* TO root@'%'` |
| HDFS put 失败 | NameNode 未启动或 safemode | `hdfs dfsadmin -safemode leave` |
| 生成器中文字符乱码 | shell 环境 LANG 非 UTF-8 | `export LANG=en_US.UTF-8 LC_ALL=en_US.UTF-8` |
| 大屏数据不滚动 | 生成器没加 --loop | 启动命令必须含 `--loop` 参数 |
| Spark UI 看不到作业 | 端口冲突（4040 被占→4041） | `http://master0:4040` 和 `http://master0:4041` |

---

## 七、vserver 生产部署（100.74.215.12）

### 7.1 首次部署

```bash
# 安装 Nginx（通过 master0 中转）
ssh root@100.84.184.73
ssh root@vserver "apt-get update && apt-get install -y nginx openjdk-8-jdk"
ssh root@vserver "mkdir -p /opt/ruoyi-ui /opt/ruoyi"

# 配置 Nginx（80→前端dist + /prod-api/→后端8080）
cat > /tmp/nginx-ruoyi.conf << 'EOF'
server {
    listen 80;
    root /opt/ruoyi-ui/dist;
    index index.html;
    location /dev-api/ { proxy_pass http://127.0.0.1:8080/; proxy_set_header Host $host; }
    location /prod-api/ { proxy_pass http://127.0.0.1:8080/; proxy_set_header Host $host; }
    location / { try_files $uri $uri/ /index.html; }
    location /static/ { expires 30d; add_header Cache-Control "public"; }
}
EOF
scp /tmp/nginx-ruoyi.conf root@vserver:/etc/nginx/sites-available/ruoyi
ssh root@vserver "ln -sf /etc/nginx/sites-available/ruoyi /etc/nginx/sites-enabled/ && rm -f /etc/nginx/sites-enabled/default && systemctl enable nginx && systemctl start nginx"
```

### 7.2 日常更新

```bash
# 前端（Windows 构建后）
cd D:/neu2026bd/projects/ruoyi/ruoyi-ui && npm run build:prod && tar czf dist.tar.gz dist/
scp dist.tar.gz root@100.84.184.73:/tmp/
ssh root@100.84.184.73 "scp /tmp/dist.tar.gz root@vserver:/tmp/"
ssh root@vserver "cd /opt/ruoyi-ui && tar xzf /tmp/dist.tar.gz && systemctl reload nginx"

# 后端（Windows IDEA Maven package 后）
scp ruoyi-admin.jar root@100.84.184.73:/tmp/
ssh root@100.84.184.73 "scp /tmp/ruoyi-admin.jar root@vserver:/opt/ruoyi/"
ssh root@vserver "cd /opt/ruoyi && pkill -f ruoyi-admin; sleep 2; nohup java -Xms256m -Xmx512m -jar ruoyi-admin.jar > /tmp/ruoyi.log 2>&1 &"
```

### 7.3 AI 代理（vserver 通过 master0 上外网）

```bash
# 问题：vserver 118网段防火墙封了外网 HTTPS
# 解决：master0 起 Python 代理监听 3128 端口，转发到硅基流动
# start_all.sh 自动启动代理，单次启动：
nohup python3 -c "
from http.server import HTTPServer, BaseHTTPRequestHandler
import urllib.request, ssl
class P(BaseHTTPRequestHandler):
    def do_POST(s):
        b = s.rfile.read(int(s.headers.get('Content-Length',0)))
        r = urllib.request.Request('https://api.siliconflow.cn/v1/chat/completions', data=b,
            headers={'Content-Type':'application/json','Authorization':s.headers.get('Authorization','')})
        resp = urllib.request.urlopen(r, context=ssl.create_default_context(), timeout=120)
        s.send_response(resp.status); s.send_header('Content-Type','application/json'); s.end_headers(); s.wfile.write(resp.read())
HTTPServer(('0.0.0.0',3128), P).serve_forever()
" > /tmp/proxy.log 2>&1 &
# vserver 后端 application.yml: siliconflow.api.url=http://100.84.184.73:3128/v1/chat/completions
```

### 7.4 vserver 验证

```bash
ssh root@vserver "ps aux | grep ruoyi-admin | grep -v grep"     # 后端进程
ssh root@vserver "curl -sI http://localhost/ | head -1"          # Nginx 前端
ssh root@vserver "curl -s http://localhost:8080/analysis/abyss/quality | head -c 100"  # API
ssh root@vserver "curl -s http://localhost/dev-api/analysis/abyss/quality | head -c 100"  # 通过 Nginx 代理
```

## 八、各节点责任一览

```
Windows本地:  写代码、IDEA启动RuoYi后端、npm启动前端、scp传文件
master0:      **start_all.sh 一键全启动**：Java生成器 + Spark Streaming + 离线管道 + 监控
Middleware:   MySQL/Redis/Kafka/ZK + monitor_collector
slave1/2:     Spark Worker + DataNode + NodeManager
backup:       Spark Worker + DataNode
vserver:      部署RuoYi后端jar（生产环境）
```
